#include <generation/PriorityGenerator.h>
#include <core/base/node/Node.h>

namespace cg {
    std::string PriorityDispatcher::generate(
        const core::Node* ent,
        bool declaration,
        const core::Node* scoup,
        const IGenerator* dispetcher
    ) {

        return "";
    }
}